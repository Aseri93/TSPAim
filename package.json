{
  "name": "aim-trainer",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check-all": "npm run build && npm run test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0",
    "preact": "^10.16.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vitest": "^4.0.16"
  }
}
