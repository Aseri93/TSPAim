{
  "name": "aim-trainer",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "check-all": "npm run build && npm run test && npm run test:e2e"
  },
  "dependencies": {
    "preact": "^10.16.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@preact/preset-vite": "^2.5.0",
    "@supabase/supabase-js": "^2.89.0",
    "@types/node": "^25.0.3",
    "dotenv": "^17.2.3",
    "typescript": "^5.0.2",
    "vite": "^6.4.1",
    "vitest": "^4.0.16"
  }
}
